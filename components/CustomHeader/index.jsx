import { View } from "react-native";

// ! не используется

const CustomHeader = ({ client }) => {
  return (
    <View
      style={{
        width: "100%",
        height: "10%",
        opacity: 0.2,
      }}
    />
  );
};

export default CustomHeader;
