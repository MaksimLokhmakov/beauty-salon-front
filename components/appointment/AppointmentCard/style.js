import { StyleSheet } from "react-native";

const Card = StyleSheet.create({
  Wrapper: {
    paddingHorizontal: 25,
    paddingTop: 10,
    marginBottom: 10,
    backgroundColor: "#fff",
    borderRadius: 16,
  },
});

export default Card;
