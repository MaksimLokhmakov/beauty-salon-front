import { StyleSheet } from "react-native";

const modalWrapper = StyleSheet.create({
  wrapper: {
    flex: 1,
    opacity: 0.4,
    backgroundColor: "#fff",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  },
});

export default modalWrapper;
